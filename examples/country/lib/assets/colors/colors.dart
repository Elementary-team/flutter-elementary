import 'package:flutter/material.dart';

//ignore_for_file: public_member_api_docs

/// Semantic aliases.
/// Replenished as needed.
/// Use only the main palette.
const Color transparentColor = _transparent;
const Color whiteColor = _white;
const Color primaryColor = _deepSkyBlue;
const Color accentColor = _lightCyan;
const Color errorColor = _alizarin;
const Color backgroundColor = _white;
const Color dividerColor = _silver;

/// Buttons
const Color menuButtonColor = _merlin;

/// Text semantic aliases
const Color textColorPrimary = _nero;
const Color textColorSecondary = _merlin;
const Color textColorLight = _white;

/// Icons colors
const Color iconsPrimaryColor = _white;

// Main palette (http://www.color-blindness.com/color-name-hue/).
const Color _alizarin = Color(0xFFE31E24);
const Color _white = Colors.white;
const Color _silver = Color(0xFFBCBCBC);
const Color _merlin = Color(0xFF53524B);
const Color _nero = Color(0xFF252525);
const Color _deepSkyBlue = Color(0xFF04B4E5);
const Color _lightCyan = Color(0xFFCCF3FF);
const Color _transparent = Colors.transparent;
