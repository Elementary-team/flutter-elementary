name: counter
description: A new Flutter project.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  elementary:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  mjk_lint_rules: ^2.0.0

flutter:
  uses-material-design: true
