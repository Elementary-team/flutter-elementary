name: helper_example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  elementary_helper:
    path: ../

dev_dependencies:
  mjk_lint_rules: ^2.0.0

flutter:
  uses-material-design: true